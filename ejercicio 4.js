let edad = 25;
let tienelicencia = true;

// Verifica si la persona es mayor de 18 y tiene licencia
let puedeConducir = (edad >= 18 && tienelicencia) ? "Puedes conducir" : "No puedes conducir";

console.log(puedeConducir);
